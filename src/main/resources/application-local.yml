spring:
  profiles:
    active: local

  datasource:
    url: jdbc:mysql://localhost:3306/wafflytime_local_db?serverTimezone=UTC
    username: root
    password: wafflytime_local_pwd
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        show_sql: true
    hibernate:
      ddl-auto: update